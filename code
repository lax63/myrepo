int orderId = transactionDTO.getOrder().getOrderId();
		LOGGER.info("Recieved request for pay for order :" + orderId + " of customer : "
				+ customerEmailId);
		ResponseEntity<OrderDTO> orderResponse = template.getForEntity(
				"http://CustomerMS/Ekart//customerorder-api/order/" + orderId, //getOrderDetailsFrom CustomermS
				OrderDTO.class);
		OrderDTO orderDTO = orderResponse.getBody();
		Double totalPrice = orderResponse.getBody().getTotalPrice();
		transactionDTO.setOrder(orderDTO);
		transactionDTO.setTotalPrice(totalPrice);
		transactionDTO.setTransactionDate(orderDTO.getDateOfOrder());
		transactionDTO = paymentService.authenticatePayment(customerEmailId, transactionDTO);
		paymentService.addTransaction(transactionDTO);
		String transactionStatus = transactionDTO.getTransactionStatus().name();
		paymentCircuitBreakerService.updateOrderAfterPayment(orderId, transactionStatus);
		String message = environment.getProperty("PaymentAPI.TRANSACTION_SUCCESSFULL_ONE")+ orderId + 
				environment.getProperty("PaymentAPI.TRANSACTION_SUCCESSFULL_TWO") + orderId +
				environment.getProperty("PaymentAPI.TRANSACTION_SUCCESSFULL_TWO") + transactionDTO.getTransactionId();

		return new ResponseEntity<String>(message,HttpStatus.OK);
